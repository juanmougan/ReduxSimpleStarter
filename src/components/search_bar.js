import React from 'react';

// Functional component: literally, a function
// Class component, instead, has internal record keeping
const SearchBar = () => {
	return <input />;
}

export default SearchBar;
